#include<stdio.h>
#include<stdlib.h>

int* hash(void);

int main(void)
{
    int n = 0;
    scanf("%d\n",&n);

    
    return 0;
}

int** hash(void)
{
    table = (int*)malloc(13*sizeof(int*));
    for(int i=0; i<13; i++)
    {
        column_i = (int*)malloc(10*sizeof(int));
        for (int j=0; j<13; j++)
        {
            *(column_i+j) = 
        }
    }
    return table;
}